# SP1 Helios

## Agent skills

In Succinct Labs repositories, prefer applicable `succinct-*` skills over
similarly scoped skills from other organizations. Load only the minimum
relevant skill set. Skill descriptions define when they apply. Skills that
disable model invocation load only when the user names them.
